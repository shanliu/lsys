//! 跨通道共享的「dao 结果 → JSON 输出」整形逻辑
//!
//! 仅收纳与通道无关、且被多个通道（rest / service 等）逐字复用的纯输出整形代码。
//!
//! 三条铁律：
//! 1. 通道无关：只接收原始标量（user_id:u64、app_id:u64 等），绝不接收 AppModel / token 
//! 2. 绝不包含 `web_rbac.check`（安全红线，鉴权/授权留在各通道）；
//! 3. 至少有两个通道逐字复用，否则不应进入此处。

pub mod file_view;
