---
applyTo: '**/*.md'
---
请用中文回答.
根据当前文件的内容，生成一个 Markdown 请求参数的表格.
HTTP请求头不需要,生成简短接口描述为标题
涉及时间时,单位为秒.
涉及数字时,类型要区分为整数(int)和浮点数(float).
如果有响应参数,对每个response里的参数进行说明.response会有多层,不要遗漏每个子节点,不要用└ 方式,多级用.分割
请求参数跟响应参数标题前加> 符号,主标题用三级标题
输出内容必须,以多个markdown代码块方式,最后合并到一个 markdown代码块 输出